config:
  security.nesting: "true"
  security.syscalls.intercept.mknod: "true"
  security.syscalls.intercept.setxattr: "true"
description: "OpenClaw: nested containers allowed (required for rootless Podman inside Incus)"
devices: {}
name: openclaw-nesting
